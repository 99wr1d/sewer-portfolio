/**
 * TAILWIND.CONFIG.TS — Конфигурация Tailwind CSS
 *
 * Здесь настраивается дизайн-система проекта:
 * - Цветовая палитра (colors)
 * - Шрифты (fontFamily)
 * - Размеры текста (fontSize)
 * - Отступы (spacing)
 * - Тени (boxShadow)
 * - Анимации (animation, keyframes)
 *
 * Использование в коде:
 * - Цвета: bg-primary-600, text-cream-100
 * - Шрифты: font-serif, font-sans
 * - Тени: shadow-soft, shadow-soft-lg
 *
 * Документация: https://tailwindcss.com/docs/configuration
 */

import type { Config } from 'tailwindcss'

export default {
  // =============================================================================
  // CONTENT — Где искать классы Tailwind
  // Tailwind сканирует эти файлы и включает только используемые классы
  // =============================================================================
  content: [
    './components/**/*.{js,vue,ts}',
    './layouts/**/*.vue',
    './pages/**/*.vue',
    './plugins/**/*.{js,ts}',
    './app.vue',
    './error.vue',
    './src/**/*.{js,vue,ts}',
  ],

  // =============================================================================
  // THEME — Настройка дизайн-системы
  // extend — добавляет к стандартным значениям Tailwind, не заменяет их
  // =============================================================================
  theme: {
    extend: {
      // =========================================================================
      // ЦВЕТА
      // Использование: bg-primary-600, text-cream-100, border-warm-300
      // Числа 50-950 — оттенки от светлого к тёмному
      // =========================================================================
      colors: {
        // Основной цвет — тёплый коричневый (для кнопок, акцентов)
        primary: {
          50: '#faf7f5',
          100: '#f5ede8',
          200: '#ead9ce',
          300: '#dcc0ad',
          400: '#cba085',
          500: '#be8768',
          600: '#b1745b', // Основной оттенок для кнопок
          700: '#935f4c',
          800: '#784f42',
          900: '#624339',
          950: '#34211c',
        },

        // Вторичный цвет — нейтральный коричневый (для текста)
        secondary: {
          50: '#f8f6f4',
          100: '#efebe5',
          200: '#ded5c9',
          300: '#c9b9a7',
          400: '#b39a83',
          500: '#a3836a',
          600: '#96735e',
          700: '#7d5e4f',
          800: '#674e44',
          900: '#554239', // Основной цвет текста
          950: '#2d221d',
        },

        // Кремовый — для фонов и карточек
        cream: {
          50: '#fefdfb',
          100: '#fdf9f3',
          200: '#faf3e6',
          300: '#f5e8d3',
          400: '#edd8b8',
          500: '#e4c79d',
          600: '#d4ab74',
          700: '#c08f52',
          800: '#9e7443',
          900: '#7f5f3a',
          950: '#45311d',
        },

        // Тёплый — для секций и градиентов
        warm: {
          50: '#fdfcfb', // Основной фон страницы
          100: '#fbf8f5',
          200: '#f6f0e8',
          300: '#efe4d6',
          400: '#e5d3bd',
          500: '#d9bfa0',
          600: '#c9a57c',
          700: '#b48a5c',
          800: '#95714d',
          900: '#7a5d41',
          950: '#412f21',
        },
      },

      // =========================================================================
      // ШРИФТЫ
      // Использование: font-serif (заголовки), font-sans (текст)
      // =========================================================================
      fontFamily: {
        // Элегантный шрифт с засечками для заголовков
        serif: ['Cormorant Garamond', 'Georgia', 'serif'],
        // Современный шрифт без засечек для текста
        sans: ['Inter', 'system-ui', 'sans-serif'],
      },

      // =========================================================================
      // РАЗМЕРЫ ТЕКСТА
      // Кастомные размеры для больших заголовков
      // Использование: text-display-lg, text-display, text-display-sm
      // =========================================================================
      fontSize: {
        // Огромный заголовок (4.5rem = 72px)
        'display-lg': ['4.5rem', { lineHeight: '1.1', letterSpacing: '-0.02em' }],
        // Большой заголовок (3.5rem = 56px)
        'display': ['3.5rem', { lineHeight: '1.15', letterSpacing: '-0.02em' }],
        // Средний заголовок (2.5rem = 40px)
        'display-sm': ['2.5rem', { lineHeight: '1.2', letterSpacing: '-0.01em' }],
      },

      // =========================================================================
      // ОТСТУПЫ
      // Дополнительные значения spacing
      // Использование: p-18, m-22, gap-30
      // =========================================================================
      spacing: {
        '18': '4.5rem', // 72px
        '22': '5.5rem', // 88px
        '30': '7.5rem', // 120px
      },

      // =========================================================================
      // СКРУГЛЕНИЯ
      // Использование: rounded-4xl
      // =========================================================================
      borderRadius: {
        '4xl': '2rem', // 32px — очень скруглённые углы
      },

      // =========================================================================
      // ТЕНИ
      // Мягкие тени для элегантного вида
      // Использование: shadow-soft, shadow-soft-lg
      // =========================================================================
      boxShadow: {
        // Лёгкая тень для карточек
        'soft': '0 2px 15px -3px rgba(0, 0, 0, 0.07), 0 10px 20px -2px rgba(0, 0, 0, 0.04)',
        // Более заметная тень для hover эффектов
        'soft-lg': '0 10px 40px -15px rgba(0, 0, 0, 0.1)',
      },

      // =========================================================================
      // АНИМАЦИИ
      // Использование: animate-fade-in, animate-slide-up
      // =========================================================================
      animation: {
        'fade-in': 'fadeIn 0.5s ease-out forwards',
        'slide-up': 'slideUp 0.6s ease-out forwards',
      },

      // Определения анимаций (keyframes)
      keyframes: {
        fadeIn: {
          '0%': { opacity: '0' },
          '100%': { opacity: '1' },
        },
        slideUp: {
          '0%': { opacity: '0', transform: 'translateY(20px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
      },
    },
  },

  // Плагины Tailwind (пока не используются)
  plugins: [],
} satisfies Config
